exports.run = {
   usage: ['cilik'],
   category: 'owner',
   async: async (m, {
      client,
      args,
      command,
      setting
   }) => {
     client.reply(m.chat, `*123121231212323gtjyjkuil*`, m)
const cooldown = new Date(turned.cooldown + 5000)
      if (new Date - turned.cooldown < 5000) return client.reply(m.chat, `*mati kau*`, m)
   }
}